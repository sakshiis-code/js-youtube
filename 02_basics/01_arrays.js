// array

const myArr = [0, 1, 2, 3, 4, 5]
const makeupItems = ["Concealer", "Blush", "Foundation"]

const giArr = new Array(30, 15, 20)
console.log(myArr[1]);

// Array methods

myArr.push(6)
myArr.push(7)
myArr.pop()
console.log(myArr);
